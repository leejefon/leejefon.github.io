/*
 * UiReducer
 *
 * @author: Jeff Lee
 * @createdAt: 2017/06/09
 */

const initialState = {

};

function UiReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default UiReducer;
